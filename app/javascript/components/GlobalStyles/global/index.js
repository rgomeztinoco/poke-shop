import { imports } from "./imports";
import { layout } from "./layout";
import { reset } from "./reset";
import { typography } from "./typography";

export const global = [imports, reset, typography, layout];
